import "./globals.css";

export const metadata = {
  title: "JSR Executive Transportation",
  description: "Luxury Executive Ground Transportation",

  icons: {
    icon: "/images/logo.png",
    shortcut: "/images/logo.png",
    apple: "/images/logo.png",
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}